subdir('icons')
subdir('mime')

install_data(
    '@0@.desktop'.format(app_id),
    install_dir: datadir_path / 'applications',
)

install_data(
    '@0@.metainfo.xml'.format(app_id),
    install_dir: datadir_path / 'metainfo',
)
